import styled from "styled-components";

const Wrapper = styled.div`
  .logo {
    display: flex;
    align-items: center;
  }
  .logo-img {
    max-height: 2.5rem;
    max-width: 2.5rem;
  }
  .logo-txt {
    max-height: 3.5rem;
    margin-top: -0.5rem;
  }
`;

export default Wrapper;
